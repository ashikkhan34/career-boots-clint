/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['i.ibb.co.com'], // add the hostname of your image URL
  },
};

export default nextConfig;


